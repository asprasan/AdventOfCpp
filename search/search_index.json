{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Advent of C++","text":"<p>This is the documentation for Advent of C++, a C++ learning project.</p> <p>Here, we will try to solve the puzzles in Advent of Code using C++. We will try to cover starting from 2015.</p>"},{"location":"#useful-links","title":"Useful links","text":"<ul> <li>Advent of Code</li> <li>Advent of C++ GitHub Repository</li> <li>2015 Solutions</li> </ul>"},{"location":"2015/","title":"Solutions for Advent of Code 2015","text":"<p>Here you will find solutions to the 2015 puzzles. You can also find the original puzzles on Advent of Code. The solutions are organized by day, and each solution is in a separate file named <code>dayXX.cpp</code>, where <code>XX</code> is the day number.</p>"},{"location":"2015/day01/","title":"Day 1","text":"<p>Problem summary: Given a string of parentheses, determine the final floor Santa ends up on. An opening parenthesis <code>(</code> means going up one floor, and a closing parenthesis <code>)</code> means going down one floor.</p>"},{"location":"2015/day01/#part-1-solution","title":"Part 1 solution","text":"<p>Once the input string is read, we can iterate through each character and keep track of the current floor. We start at floor 0, and for each <code>(</code> we add 1 to the floor, and for each <code>)</code> we subtract 1 from the floor. After processing the entire string, the final value of the floor variable will be the answer.</p> <pre><code>// code snipped to check for the parantheses and calculate the final floor\n// ... \n    while (getline(file, str)){ // read the input line by line\n        for (const char c : str){ // iterate through each character in the line\n            if (c=='('){\n                soln1++;\n            }\n            else if (c==')'){\n                soln1--;\n            }\n        }\n    }\n    std::cout&lt;&lt;\"Santa's final floor: \"&lt;&lt;soln1&lt;&lt;std::endl;\n// soln1 now contains the final floor number\n</code></pre>"},{"location":"2015/day01/#part-2-solution","title":"Part 2 solution","text":"<p>In the second part of the problem, we need to determine the position of the first character in the input string that causes Santa to enter the basement (floor -1). We can modify our previous solution to keep track of the current floor and also check if it becomes -1 at any point. If it does, we can return the index of that character.</p> <pre><code>// Update the previous snippet to \n// ... \n    while (getline(file, str)){\n        for (const char c : str){\n            if (c=='('){\n                soln1++;\n            }\n            else if (c==')'){\n                soln1--;\n            }\n            inp_counter++;\n            if (soln1==-1 &amp;&amp; soln2==0){\n                soln2=inp_counter;\n            }\n        }\n    }\n    std::cout&lt;&lt;\"Santa's final floor: \"&lt;&lt;soln1&lt;&lt;std::endl;\n    std::cout&lt;&lt;\"First time Santa reaches the basement: \"&lt;&lt;soln2&lt;&lt;std::endl;\n</code></pre>"},{"location":"2015/day01/#what-i-learned","title":"What I learned?","text":""},{"location":"2015/day01/#reading-files-in-c","title":"Reading files in C++","text":"<p>In C++, we can read files using the <code>&lt;fstream&gt;</code> library. We create an <code>ifstream</code> object to read from a file, and we can use the <code>getline</code> function to read the file line by line. This allows us to process large files without loading the entire content into memory at once.</p> <pre><code>#include &lt;fstream&gt;\n\nint main() {\n    std::ifstream file(\"input.txt\");\n    std::string str;\n    while (getline(file, str)) {\n        // Process the line\n    }\n    return 0;\n}\n</code></pre>"},{"location":"2015/day01/#iterating-through-characters-in-a-string","title":"Iterating through characters in a string","text":"<p>In C++, we can iterate through each character in a string using a range-based for loop. This allows us to easily access each character and perform operations based on its value.</p> <pre><code>std::string str = \"(()())\";\nfor (const char c : str) {\n    if (c == '(') {\n        // Do something for opening parenthesis\n    } else if (c == ')') {\n        // Do something for closing parenthesis\n    }\n}\n</code></pre>"},{"location":"2015/day02/","title":"Day 2","text":"<p>Problem summary: Given an input file containing dimensions of boxes, in the form <code>LxWxH</code>, calculate the total amount of wrapping paper needed to wrap all the boxes, and the total amount of ribbon needed to wrap all the boxes.</p>"},{"location":"2015/day02/#the-basics","title":"The basics","text":"<p>Each line in the file is a string in the format <code>LxWxH</code>. Before we do any calculations, we need to parse this string to extract the length, width, and height of the box. We can do this by splitting the string on the 'x' character and converting the resulting substrings to integers.</p> <pre><code>#include &lt;iostream&gt;\n#include &lt;sstream&gt;\n#include &lt;vector&gt;\n// Function to split a string by a delimiter and convert the parts to integers\nstd::vector&lt;int&gt; split(std::string&amp; s, const char delim){\n    std::vector&lt;int&gt; result;\n    std::stringstream ss (s); // Create a stringstream from the input string LxWxH\n    std::string item;\n\n    while (std::getline(ss, item, delim)){\n        result.push_back(std::stoi(item)); // Convert the substring to an integer and add it to the result vector\n    }\n    return result;\n}\n</code></pre>"},{"location":"2015/day02/#part-1-solution-wrapping-paper-required","title":"Part 1 solution -- Wrapping paper required","text":"<p>Once, we have the dimensions as integers, it is then easy to calculate the warpping paper needed for each box. The formula for the wrapping paper needed is: <code>2*l*w + 2*w*h + 2*h*l + min(l*w, w*h, h*l)</code></p> <pre><code>int get_area(std::vector&lt;int&gt; dims){\n    int curr_area=0;\n    int l=dims[0], w=dims[1], h=dims[2];\n    std::vector&lt;int&gt; areas={l*w, w*h, l*h};\n    int min_area=areas[0];\n\n    for (int area:areas){\n        if (area&lt;min_area){\n            min_area=area;\n        }\n        curr_area += 2*area;\n    }\n    return curr_area + min_area;\n}\n\n//...\nint main(){\n    // declare variables ...\n    // ...\n    while (std::getline(file, line)){\n        dimensions = split(line, 'x');\n        sheet_area += get_area(dimensions);\n    }\n    std::cout&lt;&lt;\"Part 1: Required wrapping sheet area= \"&lt;&lt;sheet_area&lt;&lt;std::endl;\n</code></pre>"},{"location":"2015/day02/#part-2-solution-ribbon-length-required","title":"Part 2 solution -- Ribbon length required","text":"<p>The formula for the ribbon length needed is: <code>2*(min(l+w, w+h, h+l)) + l*w*h</code></p> <pre><code>int get_ribbon(std::vector&lt;int&gt; dims){\n    int l=dims[0], w=dims[1], h=dims[2];\n    std::vector&lt;int&gt; perims={2*(l+w), 2*(w+h), 2*(l+h)};\n    int min_perim=perims[0];\n\n    for (int perim:perims){\n        if (perim&lt;min_perim){\n            min_perim=perim;\n        }\n    }\n    return min_perim + (l*w*h);\n}\n\n\n//...\nint main(){\n    // declare variables ...\n    // ...\n    while (std::getline(file, line)){\n        dimensions = split(line, 'x');\n        ribbon_length += get_ribbon(dimensions);\n    }\n    std::cout&lt;&lt;\"Part 2: Required ribbon length= \"&lt;&lt;ribbon_length&lt;&lt;std::endl;\n</code></pre>"},{"location":"2015/day02/#what-i-learned","title":"What I learned?","text":""},{"location":"2015/day02/#declaring-vectors-in-c","title":"Declaring vectors in C++","text":"<p>In C++, you can declare a vector using the <code>std::vector</code> template class. You need to specify the type of elements that the vector will hold. For example, to declare a vector of integers, you can use:</p> <pre><code>std::vector&lt;int&gt; myVector;\n// and then you can add elements to the vector using the push_back method\nmyVector.push_back(10);\nmyVector.push_back(20);\n</code></pre> <p>This creates an empty vector that can hold integers. You can also initialize the vector with a specific size and default value:</p> <pre><code>std::vector&lt;int&gt; myVector(5, 0); // Creates a vector of size 5, initialized with 0\n</code></pre>"},{"location":"2015/day02/#using-stringstream","title":"Using stringstream","text":"<p>The <code>std::stringstream</code> class in C++ is a part of the <code>&lt;sstream&gt;</code> header and is used for input and output operations on strings. It allows you to treat a string as if it were a stream, which can be useful for parsing and formatting strings. To use <code>std::stringstream</code>, you can create an instance of it and then use the stream insertion (<code>&lt;&lt;</code>) and extraction (<code>&gt;&gt;</code>) operators to read from or write to the string. For example:</p> <pre><code>#include &lt;iostream&gt;\n#include &lt;sstream&gt;\n\nint main() {\n    std::string input = \"10 20 30\";\n    std::stringstream ss(input);\n    int a, b, c;\n\n    // Extract integers from the stringstream\n    ss &gt;&gt; a &gt;&gt; b &gt;&gt; c;\n\n    std::cout &lt;&lt; \"Extracted values: \" &lt;&lt; a &lt;&lt; \", \" &lt;&lt; b &lt;&lt; \", \" &lt;&lt; c &lt;&lt; std::endl;\n    return 0;\n}\n</code></pre> <p>In this example, we create a <code>std::stringstream</code> object <code>ss</code> initialized with the string \"10 20 30\". We then use the extraction operator (<code>&gt;&gt;</code>) to read the integers from the stringstream into the variables <code>a</code>, <code>b</code>, and <code>c</code>. Finally, we print the extracted values to the console.</p>"},{"location":"2015/day03/","title":"Day 3","text":"<p>Problem summary: Santa is delivering presents to an infinite two-dimensional grid of houses. He moves based on a series of directions (^, v, &lt;, &gt;) and we need to determine how many unique houses receive at least one present. In part 2, Santa is joined by Robo-Santa, and they take turns moving based on the same series of directions. We need to determine how many unique houses receive at least one present from either Santa or Robo-Santa.</p>"},{"location":"2015/day03/#the-basics","title":"The basics","text":"<p>To solve this problem, we need to keep track of the positions of Santa and Robo-Santa as they move based on the given directions. We can use a set to store the unique positions of the houses that receive presents. We will use tuples to represent the coordinates of the houses, where (x, y) represents the position on the grid.</p> <p>We can define tuples as follows:</p> <pre><code>#include &lt;tuple&gt;\nstd::set&lt;std::tuple &lt;int, int&gt;&gt; house_grid1, house_grid2; //define the grid of visited homes\nstd::tuple&lt;int, int&gt; house={0,0}; // define the santa's location for part 1\nstd::tuple&lt;int, int&gt; santa_house={0,0}, robo_house={0,0}; //define the santa's or robot's location for part 2\n</code></pre>"},{"location":"2015/day03/#defining-a-function-to-move-santa-and-robo-santa","title":"Defining a function to move Santa and Robo-Santa","text":"<p>We can define a function that takes the current position and the direction as input and returns the new position after moving in that direction. The function can be defined as follows:</p> <pre><code>std::tuple&lt;int, int&gt; next_house(std::tuple&lt;int, int&gt; house, const char c){\n    // Compute the next house visited by Santa/robot for delivering gifts\n    // If &lt; move Left\n    // If &gt; move Right\n    // If ^ move Up\n    // If v move Down\n    if (c=='&lt;'){\n        std::get&lt;0&gt;(house)--;\n    }\n    else if (c=='&gt;'){\n        std::get&lt;0&gt;(house)++;\n    }\n    else if (c=='^'){\n        std::get&lt;1&gt;(house)++;\n    }\n    else if (c=='v'){\n        std::get&lt;1&gt;(house)--;\n    }\n    return house;\n}\n</code></pre>"},{"location":"2015/day03/#part-1-solution-unique-houses-visited-by-santa","title":"Part 1 solution -- Unique houses visited by Santa","text":"<p>At each step, we will update Santa's position based on the direction and add the new position to the set of visited houses. Then for each addition, we can update the counter of unique houses visited.</p> <pre><code>while (std::getline(file, line)){\n        for (const char c: line){\n            // solution for part 1\n            house=next_house(house, c);\n            if (!house_grid1.count(house)){\n                soln1++; //update counter of unique houses visited\n                house_grid1.insert(house);\n            }\n        }\n}\nstd::cout&lt;&lt;\"Solution for part 1: \"&lt;&lt;soln1&lt;&lt;std::endl;\n</code></pre>"},{"location":"2015/day03/#part-2-solution-unique-houses-visited-by-santa-and-robo-santa","title":"Part 2 solution -- Unique houses visited by Santa and Robo-Santa","text":"<p>Here, although, we need to keep track of two positions, one for Santa and one for Robo-Santa, we just need one grid to keep track of the unique houses visited by either of them. However, we need a different grid than the one used in part 1.</p> <pre><code>while (std::getline(file, line)){\n    for (const char c: line){\n        // solution for part 2\n        if (counter%2==0){\n            // logic for santa's movements\n            santa_house=next_house(santa_house, c);\n            if (!house_grid2.count(santa_house)){\n                soln2++;\n                house_grid2.insert(santa_house);\n            }\n        }\n        else if (counter%2==1){\n            // logic for robot's movements\n            robo_house = next_house(robo_house, c);\n            if (!house_grid2.count(robo_house)){\n                soln2++;\n                house_grid2.insert(robo_house);\n            }\n        }\n        counter++; // used to check if it's santa's or robot's turn to move\n        }\n    }\nstd::cout&lt;&lt;\"Solution for part 2: \"&lt;&lt;soln2&lt;&lt;std::endl;\n</code></pre>"},{"location":"2015/day03/#what-i-learned","title":"What I learned?","text":""},{"location":"2015/day03/#declaring-a-tuple-in-c","title":"Declaring a tuple in C++","text":"<p>Tuples can be declared using the <code>std::tuple</code> template. For example, to declare a tuple that holds two integers, we can use:</p> <pre><code>#include &lt;tuple&gt;\nstd::tuple&lt;int, int&gt; my_tuple;\nstd::get&lt;0&gt;(my_tuple) = 5; // Accessing the first element\nstd::get&lt;1&gt;(my_tuple) = 10; // Accessing the second element\n</code></pre> <p>We can also store tuples with different data types, such as:</p> <pre><code>std::tuple&lt;int, std::string&gt; my_tuple;\nstd::get&lt;0&gt;(my_tuple) = 42; // Accessing the first element\nstd::get&lt;1&gt;(my_tuple) = \"Hello\"; // Accessing the second element\n// can also initialize directly\nstd::tuple&lt;int, std::string&gt; my_tuple={42, \"Hello\"};\n</code></pre>"},{"location":"2015/day03/#using-a-set-to-store-unique-values","title":"Using a set to store unique values","text":"<p>A set is a data structure that stores unique values. In C++, we can use the <code>std::set</code> container to store unique tuples representing the positions of the houses. For example:</p> <pre><code>#include &lt;set&gt;\nstd::set&lt;std::tuple&lt;int, int&gt;&gt; house_grid;\nstd::tuple&lt;int, int&gt; house = {0, 0};\nhouse_grid.insert(house); // Insert a house into the set\nif (house_grid.count(house)) {\n    // This will return true if the house is already in the set\n}\n</code></pre> <p>We could have also used a <code>std::vector</code> to store the visited houses. However, checking for the presence of a house in a vector would require iterating through the entire vector, which has a time complexity of O(n). In contrast, using a set allows us to check for the presence of a house in O(log n) time, making it more efficient for this problem.</p>"},{"location":"2015/day04/","title":"Day 4","text":"<p>Problem summary: We have to help Santa mining some AdventCoins. To do that, we need to find the lowest positive integer (starting with 1) that, when combined with a secret key and hashed using MD5, produces a hash that starts with a certain number of zeroes.</p>"},{"location":"2015/day04/#computing-md5-checksum-of-a-string","title":"Computing md5 checksum of a string","text":"<p>Here, we don't implement the MD5 algorithm ourselves. We use the functions <code>md5</code> from here. However, for completeness, let's see how the MD5 algorithm works in general.</p> <p>In MD5, we take the input message and process it in 512-bit blocks. The algorithm consists of four rounds of operations, each round involving a series of mathematical functions and bitwise operations. The output is a 128-bit hash value, which is typically represented as a 32-character hexadecimal string. The MD5 algorithm is designed to be fast and efficient, making it suitable for hashing large amounts of data. However, it is important to note that MD5 is not considered secure for cryptographic purposes, as it is vulnerable to collision attacks. Therefore, it is recommended to use stronger hashing algorithms like SHA-256 or SHA-3 for secure applications.</p>"},{"location":"2015/day04/#part-1-solution-lowest-integer-whose-md5-begins-with-00000-when-combined-with-a-secret-key","title":"Part 1 solution -- lowest integer whose MD5 begins with 00000 when combined with a secret key","text":"<p>Here, all we need to do is to iterate through positive integers, concatenate each integer with the secret key, compute the MD5 hash of the resulting string, and check if the hash starts with \"00000\". We continue this process until we find the lowest integer that satisfies this condition.</p> <pre><code>int main(){\n    // ... declarations\n    while (1){\n        std::string counter_str=std::to_string(counter);\n        std::string md5_input = secret + counter_str;\n\n        md5_output = md5(md5_input);\n        if (md5_output.substr(0,5) == \"00000\"){\n            std::cout&lt;&lt;\"Solution for part 1: \"&lt;&lt;counter&lt;&lt;std::endl;\n            break;\n        }\n        counter++;\n    }\n}\n</code></pre>"},{"location":"2015/day04/#part-2-solution-lowest-integer-whose-md5-begins-with-000000-when-combined-with-a-secret-key","title":"Part 2 solution -- lowest integer whose MD5 begins with 000000 when combined with a secret key","text":"<p>The only diference between part 1 and part 2 is that we need to check if the hash starts with \"000000\" instead of \"00000\". The implementation is similar to part 1, but we change the condition in the if statement accordingly.</p> <pre><code>int main(){\n    // ... declarations\n    while (1){\n        std::string counter_str=std::to_string(counter);\n        std::string md5_input = secret + counter_str;\n        md5_output = md5(md5_input);\n        // std::cout&lt;&lt;\"Solution for part 2: \"&lt;&lt;md5_input&lt;&lt;std::endl;\n        if (md5_output.substr(0,6) == \"000000\"){\n            std::cout&lt;&lt;\"Solution for part 2: \"&lt;&lt;counter&lt;&lt;std::endl;\n            break;\n        }\n        counter++;\n    }\n}\n</code></pre>"},{"location":"2015/day04/#what-i-learned","title":"What I learned?","text":""},{"location":"2015/day04/#including-external-libraries-in-c","title":"Including external libraries in C++","text":"<p>In C++, we can include external libraries by using the <code>#include</code> directive. For example, to include the MD5 library, we can use <code>#include \"md5.h\"</code>. This allows us to use the functions defined in the MD5 library in our code. We also need to make sure that the library is properly linked during compilation, which may involve adding the library file to our project and specifying it in the compiler settings.</p>"},{"location":"2015/day04/#string-manipulation-in-c","title":"String manipulation in C++","text":"<ol> <li>Concatenation: We can concatenate strings using the <code>+</code> operator or the <code>append()</code> method. For example, <code>std::string result = str1 + str2;</code> or <code>str1.append(str2);</code>.</li> <li>Substring: We can extract a substring from a string using the <code>substr()</code> method. For example, <code>std::string sub = str.substr(0, 5);</code> will give us the first 5 characters of the string <code>str</code>.</li> <li>Comparison: We can compare strings using the <code>==</code> operator or the <code>compare()</code> method. For example, <code>if (str1 == str2) { ... }</code> or <code>if (str1.compare(str2) == 0) { ... }</code>.</li> </ol>"}]}